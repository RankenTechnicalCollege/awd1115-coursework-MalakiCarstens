@model IEnumerable<FandomFinds.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-white"> Orders Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th class="text-white">
                @Html.DisplayNameFor(m => m.OrderName)</th>
            <th class="text-white">
                @Html.DisplayNameFor(model => model.OrderDate)
            </th>
            <th class="text-white">
                @Html.DisplayNameFor(model => model.TotalAmount)
            </th>
            <th class="text-white">
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
              <td class="text-white">@Html.DisplayFor(m => item.OrderName)</td>
            <td class="text-white">
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td class="text-white">
                @Html.DisplayFor(modelItem => item.TotalAmount)
            </td>
            <td class="text-white">
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-controller="Orders" asp-action="Edit" asp-route-id="@item.OrderId" asp-route-slug="@item.Slug(item.OrderName)">Edit</a> |
                <a asp-action="Details" asp-controller="Orders" asp-route-id="@item.OrderId" asp-route-slug="@item.Slug(item.OrderName)">Details</a> |
                <a asp-controller="Orders" asp-action="Delete" asp-route-id="@item.OrderId" asp-route-slug="@item.Slug(item.OrderName)">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
